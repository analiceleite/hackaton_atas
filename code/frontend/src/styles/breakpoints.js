const size = {
  bs: '800px',
  md: '1000px',
  bg: '1400px',
}

const breakpoints = {
  bs: `(max-width: ${size.bs})`,
  md: `(max-width: ${size.md})`,
  bg: `(max-width: ${size.bg})`
}

export default breakpoints